# from pathlib import Path
# p = Path(input('filename '))
# p.rename(p.with_suffix(f'.{input("new suffix ")}'))

import sys
import os
import pathlib

print(sys.implementation)
print(os.name)
pathlib.Path.joinpath()
